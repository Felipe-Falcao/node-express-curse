os: linux

language: node_js

node_js:
  - 7
  
script:
  - npm test
  - npm run

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  keep_history: true
  target_branch: gh-pages
  on:
    branch: master
